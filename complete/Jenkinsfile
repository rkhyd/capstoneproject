pipeline {
    agent { docker { image 'maven:3.3.3' } }
      stages {
        stage('build jar file using maven') {
      steps {
        sh 'cd gs-spring-boot-docker/complete'
        sh 'pwd'
      }
    }
  }
}
