pipeline {
  agent { customWorkspace '/var/lib/jenkins/workspace' }
    stages {
      stage('build jar file using maven') {
        steps {
          sh 'pwd'
          sh 'cd complete'
          sh 'pwd'
          sh './mvnw package'
          sh 'pwd'
        }
      }
    }
  }
